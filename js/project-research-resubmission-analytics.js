const arr=v=>Array.isArray(v)?v:[];const read=(k,d)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch{return d}};const esc=v=>String(v??'').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));const el=id=>document.getElementById(id);const decisions=arr(read('geiProjectResearchSubmissionDecisionsV615',[])),revisions=arr(read('geiProjectResearchRevisionsV616',[])),packages=arr(read('geiProjectResearchResubmissionPackagesV617',[])),events=arr(read('geiProjectResearchResubmissionLifecycleV618',[]));const unique=a=>[...new Set(a.filter(Boolean))];const latest=(a,key)=>{const m=new Map();a.forEach(x=>{const k=x[key]||x.artifactId||x.artifactTitle;if(k&&!m.has(k))m.set(k,x)});return [...m.values()]};function render(){const states=events.map(x=>x.state), artifacts=unique([...events,...packages,...revisions].map(x=>x.artifactTitle||x.artifactId));const cycles=unique(packages.map(x=>x.artifactId||x.artifactTitle)).length;const revisionsCount=revisions.length;const prepared=packages.length;el('status').textContent='📊 '+events.length+' lifecycle events • '+artifacts.length+' tracked artifact(s) • '+packages.length+' resubmission package(s)';el('stats').innerHTML=[['Decision Records',decisions.length],['Revisions',revisionsCount],['Resubmission Packages',prepared],['Lifecycle Events',events.length],['Tracked Artifacts',artifacts.length],['Resubmission Cycles',cycles]].map(x=>'<div class="stat"><b>'+x[1]+'</b>'+x[0]+'</div>').join('');const counts={};states.forEach(s=>counts[s]=(counts[s]||0)+1);el('distribution').innerHTML=Object.keys(counts).length?Object.entries(counts).map(([s,n])=>'<div class="item"><span class="tag">'+esc(s)+'</span><b> '+n+'</b> recorded event(s)</div>').join(''):'<p class="hint">No V6.18 lifecycle events recorded yet.</p>';const latestByPackage=latest(events,'packageId');const bottleneck=Object.entries(counts).sort((a,b)=>b[1]-a[1])[0];el('intelligence').innerHTML='<div class="item"><b>Most-recorded lifecycle state:</b> '+esc(bottleneck?.[0]||'None')+' ('+(bottleneck?.[1]||0)+')</div><div class="item"><b>Latest recorded state:</b> '+esc(events.slice().sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt))[0]?.state||'None')+'</div><div class="item"><b>Operational recommendation:</b> '+(revisions.length?'Review open revision cycles and determine whether another V6.17 package is needed.':'Create a V6.16 revision when a documented revision request exists.')+'</div>';const ac={};[...revisions,...packages,...events].forEach(x=>{const k=x.artifactTitle||x.artifactId||'Untitled';ac[k]=(ac[k]||0)+1});el('artifacts').innerHTML=Object.entries(ac).sort((a,b)=>b[1]-a[1]).map(([k,n])=>'<div class="item"><b>'+esc(k)+'</b><p class="hint">'+n+' related workflow record(s)</p></div>').join('')||'<p class="hint">No research activity available yet.</p>'}render();