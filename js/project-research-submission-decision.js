const arr=v=>Array.isArray(v)?v:[];const read=(k,d)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch{return d}};const esc=v=>String(v??'').replace(/[&<>"']/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[x]));const el=id=>document.getElementById(id);const tracks=arr(read('geiProjectResearchSubmissionTrackingV612',[]));let decisions=arr(read('geiProjectResearchSubmissionDecisionsV615',[])),selected=null;function renderRecords(){el('status').textContent=tracks.length?'⚖️ '+tracks.length+' submission record(s) available':'📝 No V6.12 submission records found';el('records').innerHTML=tracks.length?tracks.slice().reverse().map((x,i)=>'<div class="card"><b>'+esc(x.artifactTitle||'Untitled research')+'</b><p>Destination: '+esc(x.destination||'Not specified')+'</p><button data-i="'+i+'">OPEN DECISION RECORD</button></div>').join(''):'Create a V6.12 tracking record first.';el('records').querySelectorAll('[data-i]').forEach(b=>b.onclick=()=>{selected=tracks.slice().reverse()[+b.dataset.i];renderAll()})}function renderHistory(){const h=selected?decisions.filter(x=>x.trackingId===selected.id):decisions;el('history').innerHTML=h.length?h.slice().reverse().map(x=>'<div class="card"><b>'+esc(x.decision)+'</b><p>'+esc(new Date(x.createdAt).toLocaleString())+'</p><p>'+esc(x.notes||'No notes recorded.')+'</p></div>').join(''):'No decision records saved yet.'}function renderAll(){renderRecords();renderHistory()}el('save').onclick=()=>{if(!selected){el('msg').textContent='⚠️ Select a submission first.';return}const now=new Date().toISOString(),d={id:crypto.randomUUID?.()||String(Date.now()),trackingId:selected.id,manifestId:selected.manifestId,artifactId:selected.artifactId,artifactTitle:selected.artifactTitle,decision:el('decision').value,notes:el('notes').value.trim(),lineage:'V6.1 → V6.2 → V6.3 → V6.4 → V6.5 → V6.6 → V6.7 → V6.8 → V6.9 → V6.10 → V6.11 → V6.12 → V6.13 → V6.14 → V6.15',createdAt:now,updatedAt:now,status:'RESEARCHER RECORDED DECISION'};decisions.push(d);decisions=decisions.slice(-200);localStorage.setItem('geiProjectResearchSubmissionDecisionsV615',JSON.stringify(decisions));renderAll();el('msg').textContent='✓ V6.15 decision recorded locally. No external decision was verified.'};renderAll();