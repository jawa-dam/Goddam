import{loadState,saveState}from'./state.js';const state=loadState();state.evidenceRecords=Array.isArray(state.evidenceRecords)?state.evidenceRecords:[];const $=s=>document.querySelector(s);function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function render(){const list=$('#list');if(!state.evidenceRecords.length){list.innerHTML='<p class="hint">No evidence recorded yet. Add your first evidence item above.</p>';return}list.innerHTML=state.evidenceRecords.slice().reverse().map(r=>'<article class="source"><strong>'+esc(r.description)+'</strong><br><span class="badge">'+esc(r.type)+'</span><span class="badge">'+esc(r.source||'No reference')+'</span><p class="hint">Context: '+esc(r.context||'Not recorded')+'</p><small class="hint">Recorded '+new Date(r.createdAt).toLocaleString()+'</small></article>').join('')}$('#add').onclick=()=>{const description=$('#description').value.trim();if(!description){$('#description').focus();return}state.evidenceRecords.push({id:String(Date.now()),type:$('#type').value,description,source:$('#source').value.trim(),context:$('#context').value.trim(),createdAt:new Date().toISOString()});saveState(state);$('#description').value='';$('#source').value='';$('#context').value='';render()};render();