import{loadState}from'./state.js';const s=loadState(),$=id=>document.getElementById(id),arr=v=>Array.isArray(v)?v:[],esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const text=(...v)=>v.filter(x=>typeof x==='string'&&x.trim()).join(' '),first=(o,...keys)=>{for(const k of keys)if(o?.[k])return String(o[k]);return''};const knowledge=arr(s.knowledgeRecords),evidence=arr(s.evidenceRecords),experiments=arr(s.labTrials),investigations=arr(s.caseFiles),publications=arr(s.researchShowcase);const questions=knowledge.map(r=>first(r,'question')).filter(Boolean);const artifacts=knowledge.length+evidence.length+experiments.length+investigations.length+publications.length;const levels=s.completedLevels?.length||0;$('artifacts').textContent=artifacts;$('questions').textContent=questions.length;$('evidence').textContent=evidence.length;$('experiments').textContent=experiments.length;const card=(label,title,body,cls='')=>`<div class="step ${cls}"><b>${esc(label)}</b><h3>${esc(title)}</h3><p>${esc(body)}</p></div>`;let recommendation='';if(!artifacts)recommendation=card('START HERE','Begin with an observation.','Record something specific that you can describe clearly. Then turn that observation into a question.');else if(!questions.length)recommendation=card('NEXT MOVE','Turn an observation into a question.','Your record has artifacts, but no saved research questions. Define what you want to understand before adding more records.');else if(!evidence.length)recommendation=card('HIGH PRIORITY','Build an evidence layer.','You have recorded questions but no evidence records. Gather sources, observations, or other checkable material relevant to the questions.','warning');else if(!experiments.length)recommendation=card('HIGH PRIORITY','Test an idea.','Your record contains evidence but no documented experiments. Consider a small, clearly recorded test where appropriate.','warning');else if(!investigations.length)recommendation=card('NEXT MOVE','Open a Case File.','Turn one strong question into a structured investigation so the reasoning, evidence, and results stay connected.');else if(!knowledge.length)recommendation=card('NEXT MOVE','Create structured knowledge.','You have research activity but no knowledge records. Synthesize what the evidence and investigation currently support, without overstating the conclusion.');else recommendation=card('NEXT MOVE','Review and connect.','Your core research layers are populated. Revisit unanswered questions, compare evidence, and strengthen relationships between investigations and knowledge.');$('recommendation').innerHTML=recommendation;$('questionsList').innerHTML=questions.length?questions.slice(0,8).map((q,i)=>card(`PRIORITY ${i+1}`,q,'Use this question as a candidate starting point for your next research action.')).join(''):'<p class="empty">No saved questions yet. Your next strategic move is to create one.</p>';const foundations=[['Knowledge',knowledge.length],['Evidence',evidence.length],['Experiments',experiments.length],['Investigations',investigations.length],['Publications',publications.length]];$('foundation').innerHTML=foundations.map(([name,n])=>card(n?'ACTIVE':'MISSING',`${name}: ${n} record${n===1?'':'s'}`,n?'This layer is represented in your current research record.':'This layer is not represented yet; consider whether it is appropriate for your next stage.')).join('');const actions=[];if(questions.length)actions.push(['01','Select one question','Choose the question with the clearest next test or evidence path.']);if(!evidence.length)actions.push(['02','Collect evidence','Record sources or observations that can be checked and revisited.']);if(!experiments.length)actions.push(['03','Run a documented test','When appropriate, define what you expect, what you do, and what actually happens.']);if(!investigations.length)actions.push(['04','Build a Case File','Connect a question to evidence, experiments, and reasoning.']);if(knowledge.length)actions.push(['05','Review your knowledge','Check whether your conclusion says only what your current record supports.']);actions.push(['06','Return to the Research Cycle',`You have completed ${levels} of 6 Academy levels; use the cycle to keep moving from question to evidence to tested understanding.`]);$('actions').innerHTML=actions.map(([n,t,b])=>card(n,t,b)).join('');