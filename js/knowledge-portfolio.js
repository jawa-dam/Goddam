import{loadState}from'./state.js';const state=loadState();const $=s=>document.querySelector(s);const records=Array.isArray(state.knowledgeRecords)?state.knowledgeRecords:[];const trials=Array.isArray(state.labTrials)?state.labTrials:[];$('#records').textContent=records.length;$('#trials').textContent=trials.length;$('#tests').textContent=state.modelTest?1:0;const list=$('#recordList');if(records.length){list.innerHTML=records.slice().reverse().map((r,i)=>{const title=r.title||r.name||('Knowledge Record '+(records.length-i));const obs=r.observation||r.observations||'Not recorded';const q=r.question||r.questions||'Not recorded';const conclusion=r.conclusion||r.knowledge||r.createdKnowledge||'Not recorded';return '<article class="record"><h3>💡 '+esc(title)+'</h3><div class="meta">Knowledge Record • '+(r.createdAt?new Date(r.createdAt).toLocaleDateString():'Saved')+'</div><p><strong>Observation:</strong> '+esc(obs)+'</p><p><strong>Question:</strong> '+esc(q)+'</p><p><strong>Conclusion:</strong> '+esc(conclusion)+'</p><span class="badge">Evidence preserved</span><span class="badge">Portfolio record</span></article>'}).join('')}$('#refresh').onclick=()=>location.reload();function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}